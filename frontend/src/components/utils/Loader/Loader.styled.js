import styled from 'styled-components';

export const BtnAudioSpinStyled = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;

  width: 100%;
  height: 100%;
  left: 0;
  top: 0;
  background: rgba(0, 0, 0, 0.5);
  position: absolute;

  .audioSpin {
  }
`;

export const MainTailSpinStyled = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: -60px;

  width: 100%;
  height: 100%;
  left: 0;
  top: 0;
  background: rgba(0, 0, 0, 0.5);
  position: absolute;

  .tailSpin {
  }
`;
